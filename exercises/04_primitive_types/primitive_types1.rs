// 불리언 (`bool`)

fn main() {
    let is_morning = true;
    if is_morning {
        println!("Good morning!");
    }

    // TODO: 아래 `if` 문 전에 `is_evening`이라는 이름의 불리언 변수를 정의해봐.
    // 변수의 값은 `is_morning`의 부정(반대)이어야 해.
    // let …
    if is_evening {
        println!("Good evening!");
    }
}
